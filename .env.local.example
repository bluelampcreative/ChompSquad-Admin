# Local development — copy this file to .env.local and do not commit .env.local
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production value (set in Firebase App Hosting config, not in the repo):
# NEXT_PUBLIC_API_URL=https://api.chompsquad.app
